# KOL 批量建联产品化方案

编写日期：2026-09-22。第 1–11 节保留修改前的结构梳理和目标方案，代码行号对应梳理时的版本；第 12 节记录已实施内容及上线边界。尚未连接生产数据库、部署或测试真实投递。排期是单名熟悉项目的全栈工程师的初估，需在历史数据盘点和邮箱接入验证后调整。

## 1. 结论先行

当前功能已经具备“批量发信、IMAP 拉回信、自动匹配会话、AI 识别报价”的技术雏形，但还不是一个适合多人协作的建联系统。主要原因不是发送速度或页面样式，而是业务对象和权限边界没有建立起来：

1. 当前只有功能级权限，没有工作空间、团队、成员、资源归属和操作权限。
2. `mb_jobs` 虽然保存了 `user_id`，但多数查询和写操作没有按用户或团队过滤，知道 ID 就可能读取或修改其他人的对象。
3. `mb_threads` 以 `kol_email` 全局唯一。同一个达人被不同项目、不同人员或不同发件账号触达时，会被强行合并成一条会话线。
4. 回信只有 `handled` 布尔值，没有负责人、认领租约、协作者和操作记录；多人同时打开时会重复处理。
5. 回信侧只能生成草稿并手工复制，不能在系统内回复，因此没有完整的“谁在什么时候用哪个账号回复了什么”的审计链。
6. 发件账号、模板、历史和抑制名单都是全局资源，当前所有拥有 `mail_blaster` 权限的人都能看到或修改，账号配置接口也没有管理员或资源管理员限制。

产品化的核心方向应是：**工作空间隔离 + 个人/共享邮箱授权 + 会话负责人制 + 原账号回信 + 全链路审计**。默认进入“我的待办”：个人邮箱及会话仅本人和被授权管理者可见，共享邮箱按团队授权。团队成员身份不自动授予全部邮件的阅读权。

第一版以同一公司内多人协作为假设，只建立一个默认工作空间，优先做好个人与共享邮箱两种模式。工作空间字段为后续客户隔离保留边界，暂不建设完整多租户后台。

---

## 2. 当前结构梳理

### 2.1 页面和前端流程

入口是 `/kol-outreach`，页面位于 `templates/kol_outreach.html`，脚本拆为 `static/js/kol_outreach.js` 和 `static/js/kol_inbox.js`。

当前页面是一条 5 步长流程：

1. 选择发件账号。
2. 粘贴或上传名单并解析。
3. 查看收件人列表。
4. 编辑主题、正文、落款和整批附件。
5. 预览后开始发送。

同一页面再用页签承载“回信管理”。回信页支持状态筛选、搜索、打开会话线、标记已处理、手动认领未匹配邮件、AI 生成议价建议和回复草稿。草稿明确要求用户复制到邮箱中手工发送。

注意：当前页面的“认领”只是把未匹配来信挂到某个达人邮箱下，并不是把处理责任分配给登录用户。会话通常在匹配到回信后才创建，因此“待回复”统计不能代表所有已经发出但尚未收到回复的达人。

这个流程适合单人偶尔操作，不适合持续的建联工作：没有批次工作台、草稿恢复、联系人去重确认、任务分配、批量跟进、审批和系统内回复。

### 2.2 后端模块

- `app.py`：Flask 路由、登录态、功能权限、任务入队和页面渲染。
- `mail_blaster_service.py`：发件账号、模板、名单解析、批次、发送、附件、历史和抑制名单。
- `mail_inbox_service.py`：IMAP 增量拉取、邮件匹配、会话线、AI 分类、报价账本和收件箱查询。
- `worker.py`：真正执行 SMTP 发送、回信轮询和后台任务。
- `database.py`：Postgres 连接池和查询封装。

发送链路是“Web 创建批次/入队，Worker 执行 SMTP”。收信链路是“Worker 按账号增量 IMAP 拉取，依据 `In-Reply-To`、`References` 或收件人匹配到 `mb_items`，再写入会话线”。

### 2.3 现有主要数据表

| 对象 | 当前表 | 当前作用 | 主要问题 |
|---|---|---|---|
| 发件账号 | `mb_sender_accounts` | SMTP/IMAP 凭据、用途、额度、启用状态 | 全局共享；账号 CRUD 未限制管理员；没有团队或成员绑定 |
| 批次 | `mb_jobs` | 名单批次、模板、状态、`user_id` | 有创建人但查询、预览、发送、状态接口没有统一做归属校验 |
| 批次明细 | `mb_items` | 每个达人一行、发送状态、Message-ID | 没有工作空间/项目字段；按 ID 操作缺少权限校验 |
| 模板 | `mb_templates` | 按 `mode` 区分素材/建联 | 全局模板；没有个人、团队、版本和发布状态 |
| 发信历史 | `mb_history` | 去重、配额、审计基础 | 没有 `workspace_id`、`campaign_id`、操作者和动作类型 |
| 抑制名单 | `mb_suppression` | 退订、退信、手动拉黑 | 全局名单，缺少团队范围和来源审计 |
| 收件邮件 | `mb_inbox_messages` | IMAP 邮件、匹配结果、已处理标志 | 没有负责人、可见范围、认领租约和工作空间 |
| 会话线 | `mb_threads` | 按 KOL 邮箱聚合、状态、报价 | `kol_email` 全局唯一，跨人、跨项目、跨账号串线 |
| 报价 | `mb_quotes` | 会话报价版本 | 没有报价操作者、审批人和关联活动 |
| 任务 | `task_queue` | 后台任务 | 有 `user_id`，但资源读取仍未做同样的归属限制 |

### 2.4 当前权限现状

登录后只有 `mail_blaster` 这一功能开关。`feature_required('mail_blaster')` 只判断“是否有权限进入功能”，没有判断“是否可以操作这个账号、批次、会话或邮件”。因此需要把权限从“功能级”升级为“工作空间 + 资源级”。

---

## 3. 当前体验问题

### 3.1 发信流程

- 页面过长，名单、模板、附件、发送控制和回信混在一个入口。
- 预览或发送时会重新创建批次，用户没有清晰的“草稿批次”概念，也没有批次列表和恢复入口。
- 名单解析结果停留在浏览器内，刷新或换人接手后无法继续。
- 账号选择只告诉用户能不能发，不能按团队、项目、额度、健康度和当前负载做推荐。
- 预览只解决“这一封长什么样”，没有发送前的统一预检报告：重复联系人、已联系冷却期、缺字段、退订命中、账号额度、预计完成时间、异常域名等应一次说清楚。
- 失败和暂停仍以批次状态为主，不能快速筛出“需要重试”“需要人工修正”“已退信”的具体联系人。

### 3.2 回信处理

- 所有人看到同一批会话，没有默认负责人。
- “已处理”是一个全局布尔值，不足以表达“谁处理、处理到哪一步、下一步是什么”。
- 手动认领没有并发保护，两个用户可以同时认领或修改同一会话。
- 回信能生成草稿但不能直接回复，沟通记录会散落在个人邮箱。
- 没有联系人级时间线：首次触达、打开/回信、跟进、报价、成交、退订等事件不完整。
- 没有批量操作，例如批量分配、批量标记、批量发送跟进、批量导出。

### 3.3 管理和安全

- 发件账号池与业务用户没有关系，普通功能用户理论上可以查看、修改、删除账号配置。
- 模板、抑制名单和发信历史全局共享，无法区分团队的业务边界。
- 接口普遍按数字 ID 直接读取或更新，没有统一的 `scope`/`ownership` 查询条件。
- 缺少审计日志，无法回答“谁改了模板、谁认领了回信、谁发出了回复、谁把地址加入抑制名单”。

---

## 4. 多人使用下的隔离设计

### 4.1 推荐的隔离层级

引入三层概念：

1. **Workspace（工作空间）**：公司或业务主体，是所有数据隔离的第一层。现有系统先迁移成一个默认工作空间。
2. **Team（团队/项目组）**：工作空间内的协作边界，例如品牌组、国家组、代理商组。
3. **User（成员）**：具体操作者，负责创建、认领、回复和推进会话。

所有建联资源都必须带 `workspace_id`；需要协作边界的资源再带 `team_id`。不能只依赖 Flask session 中的 `user_id`，必须在 SQL 查询层强制带作用域条件。

### 4.2 发信账号隔离

发件账号属于工作空间，由“账号管理员”维护；普通成员不应看到密码、OAuth refresh token 或修改 SMTP/IMAP 配置。

建议增加：

- `mb_sender_accounts.workspace_id`
- `mb_sender_accounts.owner_team_id`（可为空，表示工作空间共享）
- `mb_sender_account_members(account_id, user_id, permission)`，权限取 `use / manage / audit`
- `mb_sender_accounts.mailbox_type`：`personal`、`shared`
- 账号授权拆分为 `send_new`、`reply`、`manage_credentials`；邮件阅读权仍需检查会话范围，不能由“可使用这个发件账号”推导出“能看这个账号的全部邮件”。

使用原则：

- 发信时只能选择当前工作空间且成员有 `use` 权限的账号。
- 回信默认进入账号所属团队的共享队列。
- 回复必须沿用原始线程的发件账号，不允许用户临时换成其他账号。
- 管理账号、测试 SMTP/IMAP、修改凭据和删除账号只给账号管理员或系统管理员。

账号管理员默认只维护连接和授权，不自动获得邮件正文阅读权。严格私密场景建议每人使用独立企业邮箱；如果多人同时持有同一邮箱的网页登录密码，应用内权限无法阻止他们在邮箱网站看到全部邮件。共享邮箱凭据应仅由服务端持有。显示名或邮箱别名本身不能构成隔离。

### 4.3 批次和名单隔离

建议将 `mb_jobs` 产品化为 Campaign（建联活动），增加：

- `workspace_id`
- `team_id`
- `created_by`
- `owner_user_id`
- `name`
- `campaign_status`
- `visibility`：`team`、`private`、`workspace`
- `approval_status`

默认规则：创建人和所属团队可见；工作空间管理员可见；跨团队必须显式授权。所有批次、明细、附件、导出、重发和状态接口都按这个规则过滤。

### 4.4 回信和会话隔离

不要把收件箱按个人硬拆。对同一账号的回信，采用“共享可见 + 单人负责”的模式：

- 共享可见：只有被授予该共享邮箱/活动阅读权的成员可查看；个人邮箱保持私有。团队收件箱中的“我的”只是筛选条件，后端授权才是隔离边界。
- 单人负责：每条会话只有一个 `assignee_user_id`，负责下一步跟进。
- 管理可见：团队负责人和管理员可查看全部并重新分配。
- 私密草稿：回复草稿、内部备注和议价策略只对获授权的负责人、协作者和主管可见；主管读取私有内容需明确授权并留审计。

新增字段建议：

- `mb_threads.workspace_id`
- `mb_threads.team_id`
- `mb_threads.campaign_id`
- `mb_threads.assignee_user_id`
- `mb_threads.claimed_at`
- `mb_threads.claim_expires_at`
- `mb_threads.last_action_by`
- `mb_threads.visibility`

负责人是持久业务归属，不应因为离线或租约过期自动换人。分配采用事务或带版本号的条件更新，只允许领取未分配会话或经授权转派。另设短期编辑租约防止两个人同时发送，发送时再核验 `thread_version` 和最新入站消息 ID；有人已回复或收到新信时要求刷新确认。租约过期只释放编辑占用。

### 4.5 会话唯一键的调整

当前 `kol_email UNIQUE` 会导致跨项目串线。建议拆成两层：

- **联系人 Contact**：工作空间内按标准化邮箱归一，可保留历史别名。
- **建联会话 Thread**：使用独立 ID，固定 `workspace_id + sender_account_id + campaign_id + contact_id` 归属；以首次出站消息 ID 标识具体邮件链。同一个联系人可以在不同活动或不同邮件链中拥有多条会话，不再以邮箱全局唯一。

首次准备发送时就创建会话，只有发送确认后才计入“已触达/待达人回复”。联系人历史按授权聚合展示，报价、负责人和成交状态仍属于具体活动会话。原 `kol_email UNIQUE` 必须在迁移后移除，不能仅加字段而保留旧约束。

### 4.5.1 回信路由规则

拉信先以已配置的物理 `account_id` 确定邮箱和工作空间，不能用邮件头里的收件地址作为权限依据。

1. 在同一工作空间、同一接收账号的出站记录中，先精确匹配 `In-Reply-To`，再按由近到远的 `References` 匹配。首发和系统内回复都进入统一可检索的 Message-ID 索引。
2. 无有效邮件头时，仅在该邮箱内检索联系人及时间窗口；只有唯一且无冲突的候选才能自动关联。有多个活动、多个负责人或地址变化时进入该邮箱授权范围内的“待归属”，不再全局选最近批次。
3. 归属继承匹配到的原会话及其当前负责人。换人接手后，新回信继续进入被转派的会话，不能重新分给最初创建人。
4. 未匹配邮件只允许该邮箱的授权分拣人员处理。个人邮箱交给本人，共享邮箱交给队列管理员；不能暴露为全员可见的未匹配列表。
5. 人工挂接必须同时校验消息和目标会话权限及账号一致性。跨账号转发只在经验证的映射规则下处理，不能靠发件地址猜测。
6. 去重按邮箱隔离：Message-ID 去重使用 `(account_id, message_id)`，无 Message-ID 时采用 `(account_id, folder, uidvalidity, uid)`。同一邮件被两个邮箱收到时分别保留各自副本。IMAP 游标也需账号锁，防止定时与手动轮询并发推进。

示例：A 和 B 分别从自己的邮箱联系同一个达人，回复按接收邮箱和出站 Message-ID 分别归属；A/B 共用团队邮箱联系同一达人但属于两个活动时，有邮件头就精确分流，没有邮件头且存在两个候选就进入待归属，不能随意交给最近发信的人。

### 4.6 发信、回信和审计隔离

新增统一事件表 `mb_audit_events`，记录：

- `workspace_id / team_id / actor_user_id`
- `entity_type / entity_id`
- `action`
- `before_json / after_json`
- `request_id / created_at`

新增出站消息表 `mb_outbound_messages`，不要再把系统内回复依赖“复制草稿”：

- `thread_id / account_id / user_id / parent_inbox_id`
- `to_email / cc / subject / body_text / body_html`
- `message_id / in_reply_to`
- `status`：`draft / pending_approval / queued / sent / failed / cancelled`
- `sent_at / error`

这样可以实现：同一线程只能从原账号回复、回复前审批、发送失败重试、完整审计和统一统计。

普通回复默认“人工确认后发送”，审批只作为团队可配置策略，例如超过授权报价上限才需要主管批准，不强制所有回信都审批。AI 草稿及报价建议不计入已发送报价和议价轮次，只有实际发送确认后才能推进对应业务状态；当前 `suggest_reply` 生成建议便追加 `countered` 报价，需要一并修正。

### 4.7 后端强制隔离和共同资源

- 增加独立的 `kol_outreach` 功能权限；建联与素材提交目前共用接口、账号及权限，老 `/api/mail-blaster/*` 入口必须同步加保护，避免新页面安全而旧接口仍能绕过。
- 作用域由服务端登录用户与有效成员关系生成，客户端提交的 `workspace_id/team_id` 只能是选择项，不能直接相信。成员权限变更不能等旧 session 失效才生效。
- 统一授权覆盖列表、统计、搜索、详情、预览、发送、重发、报价、AI 上下文、附件下载/挂接、任务状态、导出和错误信息。不可访问与不存在使用相同外部响应，例如统一 404。
- worker 是系统身份，但每个任务需保存资源范围和发起人；执行每次实际发送前重新检查资源归属、邮箱授权、停用状态和抑制名单。撤销授权后尚未发送的任务暂停。
- 附件字节可继续按哈希物理去重，但上传授权、文件名与业务引用需要独立记录；不能通过裸 `attachment_id` 下载或挂接其他人的文件。
- 同一公司内对相同联系人重复首联：同活动默认阻止；跨活动提示已触达并要求确认。无权查看对方会话的人只看到“已存在团队联系记录”，不泄露正文和价格。
- 明确退订默认阻止整个工作空间继续营销；手动“不再跟进”可限定活动。退订不能通过换个人或换账号绕过；解除需要记录原因。不同工作空间的名单不可互查。
- 同一个物理发件邮箱的发送间隔、配额和队列由账号维度统一控制，防止多个活动并行绕过额度；使用账号锁/原子额度预留，不能仅靠发送前查询已发数量。
- SMTP 接收与数据库提交之间不存在天然的 exactly-once 保证。重复点击以幂等键拦截，任务和明细以原子领取防并发；投递结果不确定时进入 `delivery_unknown` 待核对，不能自动当失败重发。

---

## 5. 目标产品形态

### 5.1 导航结构

把当前单页拆成四个稳定入口：

1. **活动**：活动列表、草稿、进行中、已完成、异常。
2. **联系人**：KOL 联系人、历史触达、标签、抑制状态、所属活动。
3. **共享收件箱**：未分配、我的待办、团队全部、需人工、报价中、已成交。
4. **设置**：发件账号、团队成员、模板、退订名单、发送策略和权限。

“创建活动”用向导完成，完成后回到活动详情页，而不是停留在一条很长的页面上。

### 5.2 创建活动向导

建议 6 步：

1. 活动信息：名称、品牌/项目、团队、目标、负责人。
2. 导入名单：Excel、粘贴、历史联系人、去重规则和字段映射。
3. 选发件账号：仅显示当前成员可用账号，并展示健康度、日额度、收信状态、冷却命中。
4. 内容与变量：模板、个性化字段、附件、退订设置、落款。
5. 预检与审批：重复、缺字段、抑制名单、冷却期、域名风险、预计时长和抽样预览。
6. 发送与监控：确认后入队，实时展示成功、失败、跳过、暂停和需要处理的行。

每一步都保存服务端草稿；用户可以退出后继续，其他有权限的成员也可以接手。

### 5.3 共享收件箱

左侧使用队列视图：未分配、我的、团队、需人工、报价、待跟进、已完成。列表至少展示：KOL、所属活动、发件账号、最近动作、负责人、SLA、最新报价和未读数。

会话详情分为三条线：

- 对外往来：发送和收到的邮件完整时间线。
- 业务信息：报价、意向、状态、标签和联系人字段。
- 内部协作：负责人、内部备注、下一步、审批记录。

回复按钮直接生成草稿并通过原发件账号发送；涉及报价、折扣或敏感内容时进入审批队列。草稿默认不自动发，审批通过后才入队。

### 5.4 批量跟进

为每个会话提供 `next_action_at` 和 `follow_up_stage`，支持：

- 批量筛选“3 天未回信”“已触达未回”“报价待确认”。首版不承诺可靠的打开/已读追踪。
- 选择跟进模板并预览变量。
- 按工作时间和账号额度分批发送。
- 发送前二次检查退订、硬退信、重复跟进和负责人冲突。
- 排队后若出现真实回信、退订、成交或负责人变更，发送前再次核验并取消或暂停过期跟进。

---

## 6. 分阶段实施计划

### Phase 0：先止住串数据和误操作（1 周）

目标是先把最危险的共享问题挡住，不改变主要页面。

- 所有 mail-blaster API 增加统一资源作用域函数，至少按 `mb_jobs.user_id` 校验创建人；管理员可跨用户查看。
- `mb_threads`、`mb_inbox_messages` 的读取和更新必须通过关联 job/account 做归属校验。历史线程可能已混入多人的消息，不能只用线程保存的首个 `job_id` 判断整条可见；混合或未知归属线程暂仅迁移管理员可见，拆分核对后再开放。
- 发件账号 CRUD、批量导入、测发信、测收信限制为管理员或账号管理员。
- 模板、历史、抑制名单先增加“管理员可写、普通用户只读”的临时策略。
- 对不可访问和不存在的资源统一返回 404；同时保护统计、附件、旧接口和后台任务入口。
- 回信匹配先加账号边界；发送入队和明细领取增加幂等保护，账号并发配额统一控制。
- 增加回归测试：用户 A 不能读取、发送、重发、认领用户 B 的批次和会话。

### Phase 1：建立工作空间和协作基础（2～3 周）

- 增加 `workspaces`、`workspace_members`、`teams`、成员角色。
- 所有 `mb_*` 业务表补 `workspace_id`，历史数据迁移到默认工作空间。
- 增加账号团队归属和使用权限。
- `mb_jobs` 增加活动名称、团队、负责人、可见范围。
- `mb_threads` 增加负责人、认领时间、租约和团队字段。
- 上线活动列表、我的活动、共享收件箱、认领/转派和操作日志。
- 分开存储业务阶段（未触达、已触达、洽谈、成交、放弃）、处理状态（待我方回复、待达人回复、完成）、负责人和个人已读游标，避免把“未读”“未分配”“已成交”混成一个状态。

### Phase 2：重做建联工作流（2～4 周）

- 把当前长页面改成活动向导和活动详情页。
- 服务端持久化名单、字段映射、预览和草稿，不再依赖浏览器内存。
- 增加预检报告、抽样预览、联系人级错误修复和断点续发。
- 批次支持暂停、恢复、取消、重试失败项和导出结果。
- 模板支持个人草稿、团队模板、版本、发布和回滚。

### Phase 3：系统内回复和跟进（3～5 周）

- 新增 `mb_outbound_messages`，系统内回复沿用原账号和线程头。
- 回复审批、发送队列、失败重试和发信审计。
- 增加下一步、跟进日期、自动提醒和批量跟进。
- AI 只生成分类、摘要和草稿；发送必须经过用户确认或配置好的审批规则。

### Phase 4：运营和规模化（持续迭代）

- 域名/账号健康度、投递成功率、退信率、回复率和成交漏斗。
- 团队与活动维度的成本、额度和效果统计。
- 联系人合并、别名邮箱、黑名单冲突处理和跨活动历史。
- Webhook/IMAP 事件化、队列幂等、死信队列和更细的 SLA 监控。

---

## 7. 建议的权限矩阵

| 操作 | 普通成员 | 活动负责人 | 团队负责人 | 账号管理员 | 系统管理员 |
|---|---:|---:|---:|---:|---:|
| 查看团队活动 | ✓ | ✓ | ✓ | 按授权 | ✓ |
| 创建活动 | ✓ | ✓ | ✓ | 按授权 | ✓ |
| 发送活动 | 自己创建/被授权 | ✓ | ✓ | 按授权 | ✓ |
| 认领/处理回信 | 自己/未分配 | ✓ | ✓ | 按授权 | ✓ |
| 转派会话 | — | 团队内 | 团队内 | 按授权 | ✓ |
| 发送系统内回复 | 已授权会话，人工确认 | 已授权会话，人工确认 | 团队策略 | 单独授权 | 单独授权 |
| 管理发件账号凭据 | — | — | — | ✓ | ✓ |
| 管理团队模板 | 使用 | 使用 | ✓ | 按授权 | ✓ |
| 修改全局抑制名单 | 提交申请 | 提交申请 | ✓ | ✓ | ✓ |
| 查看全量审计和统计 | 自己相关 | 活动相关 | 团队相关 | 账号相关 | 全部 |

权限实现上，角色只是默认值，最终仍应由“工作空间 + 团队 + 资源 ACL”决定；不要在路由里堆很多 `if role == ...`。

---

## 8. 关键验收标准

### 数据隔离

- 用户 A 无法通过修改 URL 中的 ID 读取或修改未授权的用户 B 的活动、联系人、会话、邮件、报价、附件和发送结果；显式授权共享的资源按授权访问。
- 不同团队使用同一个邮箱账号时，回信只进入该账号所属工作空间，并按团队规则展示。
- 同一 KOL 在不同活动中的会话不会被错误合并；系统能展示共享联系人历史，但对外线程归属清晰。
- 删除或禁用成员后，其活动和会话有明确的接管策略，不丢数据、不丢回信。

### 并发和协作

- 两人同时认领同一会话，只有一人成功，另一人得到“已被某人认领”的明确提示。
- 发送、重发、回复均具备幂等键，不因重复点击或多个 worker 并发产生重复投递；SMTP 已接收但记账失败进入待核对，不自动重发。
- 所有关键操作能追溯到操作者、时间、账号、活动和目标对象。

### 使用效率

- 活动创建中途刷新或换设备后可以继续。
- 发送前能一次看到所有阻断原因，并能定位到具体联系人行。
- 回信处理无需离开系统复制粘贴，系统内回复后能在同一时间线看到出站记录。
- 团队负责人能用“我的待办、未分配、超时、报价待处理”完成日常工作，而不需要翻全量列表。

---

## 9. 最推荐的落地顺序

先做 **Phase 0 + Phase 1**，再改页面和回复能力。理由是：如果先把页面做得更漂亮，却继续让所有人共享账号、会话、模板和收件状态，使用人数增加后只会把串信和责任不清放大。第一版产品化的完成标志不是“能发更多封”，而是任何一封信都能回答四个问题：

1. 属于哪个工作空间、团队和活动？
2. 使用哪个发件账号发出、回信应该回到哪个账号？
3. 当前由谁负责，下一步是什么？
4. 谁在什么时间做了什么操作？

这四个问题稳定后，再扩展自动跟进、AI 和运营报表，系统才适合多人长期使用。

## 10. 迁移与上线方式

1. 只读盘点：统计建联账号、批次创建人、无归属记录、跨账号混合线程、同邮箱多活动和孤立附件。列出需要人工确认的清单；不能从本次静态阅读推断生产库已经发生了多少串线。
2. 增量建表：保留 SMTP/IMAP、Postgres 和现有 worker，增加作用域、成员授权、活动归属、出站消息及审计表。使用版本化迁移，避免把大规模回填塞进启动时的 `ensure_schema()`。
3. 回填：以批次创建人、实际发件账号和 `matched_item_id` 逐封恢复来源；原混合会话按来源拆分。手动挂接、仅邮箱兜底、报价来源不明的记录进隔离区，不默认分给任何普通成员。报价有 `source_inbox_id` 时随源消息迁移，无来源建议留待核对。
4. 影子校验：新匹配规则只写对照结果，不触发发信；比较旧新归属、未匹配率、跨账号冲突和数量平衡。迁移前后已发记录、报价和附件引用必须可对账。
5. 小范围试用：先用两名用户、两个独立邮箱和一个共享邮箱验证同一达人跨账号/跨活动的全部场景。部署前暂停相关队列、排空在途发送、保存游标与备份，再切换读写路径。
6. 回退：关闭新的发送入口并保留审计与已发凭据；不回退到全员可见的旧权限模型。灰度失败可暂停发送和提供授权内只读访问，修复后续跑，不能恢复备份后重发已经实际投递的邮件。

人员离职使用停用而非直接删除：撤销授权、失效登录态、暂停排队发送、转派活动和会话、保留历史操作者。第一版如果允许在外部邮箱继续回复，需要明确外部已发消息尚不能完整自动归档；共享邮箱优先在系统内回复，Sent 文件夹/服务商 API 同步作为下一项接入工作。

## 11. 代码依据与交付边界

| 结论 | 当前代码位置 |
|---|---|
| 功能权限只检查 session 中的角色和权限项 | `app.py:975`、`app.py:982` |
| 发件账号接口无成员授权条件 | `app.py:10565`、`app.py:10606`、`mail_blaster_service.py:638` |
| 批次写了创建人，但读取/发送没有归属校验 | `mail_blaster_service.py:3380`、`app.py:10796`、`app.py:10853` |
| 预览和发送都会创建新批次 | `static/js/kol_outreach.js:217` |
| 会话按邮箱全局唯一 | `mail_blaster_service.py:371`、`mail_inbox_service.py:207` |
| 回信匹配不接收 account_id，兜底全局选最近一次 | `mail_inbox_service.py:166`、`mail_inbox_service.py:305` |
| 入站 Message-ID 全局去重 | `mail_inbox_service.py:307` |
| 收件箱列表、统计、详情和修改无用户作用域 | `app.py:11040`、`mail_inbox_service.py:832`、`mail_inbox_service.py:856` |
| 认领是绑定达人会话，未记录用户负责人 | `mail_inbox_service.py:929`、`static/js/kol_inbox.js:67` |
| 回复草稿要求用户复制后手动发送 | `templates/kol_outreach.html:241` |
| 草稿建议会推进报价记录 | `mail_inbox_service.py:751` |
| 发送明细没有原子状态领取，账号配额是查询后发送 | `mail_blaster_service.py:2989`、`mail_blaster_service.py:3125` |

首个可用版本交付范围：独立建联权限、个人/共享邮箱授权、活动列表与持久草稿、准确回信归属、我的待办、负责人转派、系统内人工确认回复、完整时间线、发送幂等和审计。先完成这些再扩展自动序列、复杂审批、多租户管理和打开追踪。

验收除前述权限用例外，至少包括：同邮箱无头回信多候选不自动归属；同 Message-ID 到两个邮箱均保留；撤权后排队任务不再发送；新回信使过期跟进失效；生成三次议价草稿不增加实际议价轮次；刷新和换设备能继续活动；旧素材提交流程不因共用接口改动而回归。

## 12. 第一批实施记录（2026-09-22）

本批交付“个人归属隔离 + 邮箱发送授权 + 活动工作台”。这是完整方案的第一批实现，尚不是工作空间/团队协作和系统内回复的完整版本。

### 12.1 已实现

- 新增 `mail_access.py`，HTTP 请求和发送 worker 从数据库重新读取用户权限；活动、会话、来信、模板、历史、附件、图片和邮件任务按归属校验。任务中心汇总也过滤他人的邮件任务。
- 管理员为每个邮箱显式勾选可发信成员。授权允许使用邮箱发信，不开放其他成员的邮件正文；管理员目前具有全局查看权，不区分凭据管理员和内容管理员。素材提交共用邮箱服务，也适用此规则。
- 回信匹配限定实际收件账号，依次使用 `In-Reply-To` 和最近的 `References`。仅邮箱地址匹配存在多个候选时，留给管理员归属，不自动选最近活动。线程绑定原始发送明细，同达人跨活动分别保存。
- 入站去重使用账号及 Message-ID；缺少 Message-ID 时使用账号、文件夹、UIDVALIDITY、UID。同一 Message-ID 投递到两个邮箱会分别保留。
- 首页新增“我的活动”，可命名、保存、恢复草稿、查看发送进度和逐封状态。保存和预览复用活动；发送后锁定内容，避免重试变更原始邮件。替换名单会创建新活动，保留旧草稿。
- 入队与任务写入同一事务；同活动重复点击复用任务。同一邮箱已有排队或发送活动时拒绝新活动入队，后续可升级为按邮箱调度的队列。
- 每封发送前再次检查活动创建人的权限和邮箱授权；单封重发也检查窗口和额度。撤权不能撤回已进入 SMTP 的邮件。
- 投递连接中断、超时、进程中断等结果不明确的明细标为“投递待核实”，不会自动重发。生成议价草稿不再写入实际还价轮次。

### 12.2 数据升级与发布

版本迁移位于 `mail_access.ensure_schema()`，通过版本表、事务及 advisory lock 防止重复执行。本次修改了现有启动迁移入口，首次运行新版 web/worker 会触发版本 1；生产发布前应先评估收件表规模，预留更新去重键的时间。

1. 暂停邮件队列和定时收信，等待在途发送结束，备份数据库，再统一更新 web 与 worker，避免新旧进程同时处理邮件。
2. 在维护窗口运行并确认迁移成功；迁移新增邮箱/附件成员表、个人模板归属、活动名称，以及历史线程隔离标志。已有用户、发信明细和收件内容保留。
3. 所有旧线程默认 `legacy_locked=true`，先限制为管理员查看；没有自动拆分历史混合会话。旧模板没有可靠作者，保留为管理员可见。无创建人的历史活动同样不开放给普通成员。
4. 管理员在“邮箱与授权”中为建联和素材邮箱重新授权。迁移不会猜测或自动授予所有人权限。已有已授权活动的附件按活动归属读取，未绑定的旧上传附件不开放给普通成员。
5. 用两个测试成员验证邮箱列表、活动、回信、附件、任务隔离，再恢复队列。失败时暂停发送、保留已发记录，不能回退到旧版全员可见或恢复旧库后重发。

### 12.3 本地验证

- `test_mail_isolation.py` 使用明确限定为 localhost:55449 的临时 PostgreSQL，测试实际 SQL、Flask 路由和迁移；不导入生产应用启动流程。
- 已验证跨用户访问、共享邮箱同达人回信隔离、多候选不自动关联、附件去重后的归属、撤权后发送、重复入队、手动归属冲突、单封额度/窗口、历史数据升级、中断恢复，以及素材附件预览兼容。
- 相关回归共 42 项通过。Playwright 验证草稿保存/恢复/预览不重复建活动、Alice/Bob 隔离、管理员授权，以及 1440px 桌面和 390px 手机视图；页面无 JavaScript 错误和横向溢出。
- 本地演示：`http://127.0.0.1:5057/demo/1`（Alice）、`/demo/2`（Bob）、`/demo/3`（管理员）。启动脚本 `output/kol_outreach_preview.py` 只使用临时数据库且禁用投递/收信；不能部署该演示登录入口到生产。

### 12.4 下一批范围与当前限制

尚未实现工作空间/团队、会话转派、协作者、完整审计、系统内人工回复、Sent 文件夹同步、自动跟进和投递待核实的人工裁决界面。当前回复草稿仍需在邮箱客户端发送；共享邮箱网页登录权限无法由本系统隔离，严格私密使用应分配独立邮箱。

当前以活动创建人作为持续负责人，成员离职或资源转交需要管理员处理，暂未提供自助转派。活动列表最多显示最近 100 条，下一批补分页、筛选和检索。SMTP 不支持端到端恰好一次投递，本批通过原子领取和未知结果不重试降低重复发送风险，仍需在测试邮箱上验证服务商投递、退信和 IMAP 行为后再上线。
