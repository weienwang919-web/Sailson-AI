# FB 舆情监控 - 配置管理功能

## 新增功能

### 1. 数据库表
新增 `fb_monitor_config` 表用于存储监控配置：
- `id` - 主键
- `post_url` - Facebook 帖子链接（唯一）
- `post_title` - 备注名称
- `is_active` - 是否启用
- `created_by` - 创建者用户 ID
- `created_at` - 创建时间
- `last_scraped_at` - 最后抓取时间

### 2. API 接口

**GET /fb_config**
- 获取所有监控配置列表
- 返回配置详情（URL、标题、状态、最后抓取时间）

**POST /fb_config**
- 添加新的监控配置
- 参数：`post_url`（必填）、`post_title`（可选）
- 自动检查重复

**DELETE /fb_config/<config_id>**
- 删除指定的监控配置

**POST /fb_config/<config_id>/toggle**
- 启用/禁用指定的监控配置

### 3. 界面功能

在 FB 舆情看板顶部新增"监控配置"区域：

**添加监控：**
- 输入 Facebook 帖子链接
- 可选填写备注名称（方便识别）
- 点击"添加监控"按钮

**配置列表：**
- 显示所有已配置的监控项
- 每项显示：备注名称、帖子链接、状态、最后抓取时间
- 操作按钮：启用/禁用、删除

**自动抓取：**
- 定时任务会自动抓取所有"已启用"状态的帖子
- 抓取完成后更新"最后抓取时间"

### 4. 工作流程

1. 用户在界面添加要监控的 FB 帖子链接
2. 配置自动保存到数据库，默认启用
3. 定时任务（每 6 小时）自动读取所有启用的配置
4. 依次抓取每个帖子的评论并分析
5. 更新每个配置的最后抓取时间

### 5. 优势

- ✅ 无需修改代码或环境变量
- ✅ 支持多个帖子同时监控
- ✅ 可随时启用/禁用某个监控项
- ✅ 清晰显示每个帖子的抓取状态
- ✅ 支持备注名称，方便管理

## 使用方法

1. 访问 `/fb_dashboard`
2. 在顶部"监控配置"区域输入 Facebook 帖子链接
3. 可选填写备注名称（如"CFL 官方帖子"）
4. 点击"添加监控"
5. 配置添加成功后，定时任务会自动抓取
6. 也可以点击"手动抓取"按钮立即执行一次

## 注意事项

- Facebook 帖子链接格式示例：`https://www.facebook.com/页面名/posts/帖子ID`
- 每个链接只能添加一次（自动去重）
- 禁用的配置不会被定时任务抓取
- 删除配置不会删除已抓取的评论数据

## 兼容性

- 仍然支持通过环境变量 `FB_POST_URLS` 配置（作为备用方案）
- 优先从数据库读取配置，如果数据库为空则回退到环境变量
